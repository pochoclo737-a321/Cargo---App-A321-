import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.yourcompany.cargo",
  appName: "Cargo Distributor",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
